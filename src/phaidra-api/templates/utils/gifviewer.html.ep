<!DOCTYPE html>
<html lang="en">
 <head>
  <style type="text/css">
    body{
      height: 100%;
      padding: 0;
      margin: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      background-color: #f5f5f5;
    }
    div#viewer{
      text-align: center;
      padding: 20px;
    }
    img#gif-image{
      max-width: 90vw;
      max-height: 90vh;
      width: auto;
      height: auto;
      box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    }
  </style>
  % if (stash('favIcon')) {
    <link rel="icon" type="image/svg+xml" href="<%= stash('favIcon') %>"/>
  % }
 </head>
 <body>
   <div id="viewer">
     <img id="gif-image" src="<%= $scheme %>://<%= $baseurl %>/<%= $basepath %>/object/<%= $pid %>/octets" alt="GIF Image" />
   </div>
 </body>
</html>

