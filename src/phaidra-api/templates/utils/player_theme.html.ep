<style>
  /* Transparent so the parent page theme shows through the iframe.
     color-scheme: normal avoids a forced opaque light/dark canvas mismatch. */
  html {
    color-scheme: normal;
  }
  html,
  body {
    margin: 0;
    padding: 0;
    background: transparent;
  }
</style>
