stages:
  - testing_builds

build-phaidra-api:
  stage: testing_builds
  image:
    name: gcr.io/kaniko-project/executor:v1.9.0-debug
    entrypoint: [""]
  script:
    - /kaniko/executor
      --context "${CI_PROJECT_DIR}"
      --dockerfile "${CI_PROJECT_DIR}/dockerfiles/api_no_eofs.dockerfile"
      --no-push
